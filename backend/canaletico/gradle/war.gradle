apply plugin: "war"

bootWar {
    mainClassName = "com.prosegur.canaletico.CanaleticoApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
